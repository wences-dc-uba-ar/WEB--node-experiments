'use strict';

module.exports = function(server) {
	
  // server.dataSources.bpm.automigrate('people', function(err) {
  // 	if(err) {
  //   	console.log(err.message);
  // 	} else {
  //   	console.log('no error messages');
  // 	}
  // });
};